/// Version of the serialized visible document-index snapshot format.
///
/// Snapshot compatibility belongs to the storage layer rather than the live
/// runtime or the core document model.
pub const DOCUMENT_INDEX_VISIBLE_VERSION: i64 = 2;
